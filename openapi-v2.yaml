openapi: 3.0.3
info:
  title: Paycrest Aggregator API
  version: 1.0.0
  description: |
    OpenAPI schema for the Paycrest Aggregator API. This covers sender, provider, and general endpoints.
tags:
  - name: Sender
    description: Endpoints for senders to create and manage payment orders
  - name: Provider
    description: Endpoints for providers to manage and fulfill orders
  - name: General
    description: General protocol and utility endpoints
components:
  securitySchemes:
    ApiKeyAuth:
      type: apiKey
      in: header
      name: API-Key
  parameters:
    OptionalOrderDirection:
      name: direction
      in: query
      required: false
      schema:
        type: string
        enum: [onramp, offramp]
      description: |
        Filter to **onramp** (fiat → crypto) or **offramp** (crypto → fiat). Omit to include **both** directions.

        On **stats** endpoints, invalid values return **400 Bad Request**. On **list** endpoints, values other than `onramp` or `offramp` are ignored (no direction filter).

        **HMAC (GET):** the signed payload includes all query parameters—include `direction` when you send it (alongside `timestamp`, `currency`, etc.).
  schemas:

    # Sender Schemas
    NewPaymentOrderPayload:
      type: object
      required:
        - amount
        - token
        - rate
        - network
        - recipient
      properties:
        amount:
          type: number
        token:
          type: string
          description: Token symbol (USDT, USDC, cNGN). See [Supported Stablecoins](/resources/supported-stablecoins) for available options.
        rate:
          type: number
        network:
          type: string
          enum: [ethereum, base, bnb-smart-chain, lisk, scroll, celo, arbitrum-one, polygon, asset-chain]
          description: Network identifier for the blockchain
        recipient:
          $ref: '#/components/schemas/PaymentOrderRecipient'
        reference:
          type: string
        returnAddress:
          type: string
    PaymentOrderRecipient:
      type: object
      required:
        - institution
        - accountIdentifier
        - accountName
        - memo
      properties:
        institution:
          type: string
          description: Institution code (SWIFT code or custom PayCrest code ending with 'PC'). See [Code Standards](/resources/code-standards) for details.
        accountIdentifier:
          type: string
          description: Bank account number, mobile number, or other account identifier
        accountName:
          type: string
          description: Name of the account holder
        memo:
          type: string
          description: Memo or reference for the transaction
        providerId:
          type: string
          description: Optional provider ID for specific routing
        metadata:
          type: object
          description: Additional metadata for the transaction
        currency:
          type: string
          description: ISO 4217 currency code (e.g., NGN, KES, UGX). See [Supported Currencies](/resources/supported-currencies) for available options.
    ReceiveAddressResponse:
      type: object
      properties:
        id:
          type: string
        amount:
          type: string
        token:
          type: string
          description: Token symbol (USDT, USDC, cNGN). See [Supported Stablecoins](/resources/supported-stablecoins) for available options.
        network:
          type: string
          enum: [ethereum, base, bnb-smart-chain, lisk, scroll, celo, arbitrum-one, polygon, asset-chain]
          description: Network identifier for the blockchain
        receiveAddress:
          type: string
        validUntil:
          type: string
          format: date-time
        senderFee:
          type: string
        transactionFee:
          type: string
        reference:
          type: string
        orderType:
          type: string
    SenderOrderResponse:
      type: object
      properties:
        id:
          type: string
        amount:
          type: string
        amountInUsd:
          type: string
        amountPaid:
          type: string
        amountReturned:
          type: string
        token:
          type: string
          description: Token symbol (USDT, USDC, cNGN). See [Supported Stablecoins](/resources/supported-stablecoins) for available options.
        senderFee:
          type: string
        transactionFee:
          type: string
        rate:
          type: string
        network:
          type: string
          enum: [ethereum, base, bnb-smart-chain, lisk, scroll, celo, arbitrum-one, polygon, asset-chain]
          description: Network identifier for the blockchain
        gatewayId:
          type: string
        recipient:
          $ref: '#/components/schemas/PaymentOrderRecipient'
        fromAddress:
          type: string
        returnAddress:
          type: string
          description: Deprecated — use refundAddress
        refundAddress:
          type: string
        receiveAddress:
          type: string
        feeAddress:
          type: string
        reference:
          type: string
        orderType:
          type: string
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
        txHash:
          type: string
        status:
          type: string
        transactionLogs:
          type: array
          items:
            $ref: '#/components/schemas/TransactionLog'
    SenderPaymentOrderList:
      type: object
      properties:
        total:
          type: integer
        page:
          type: integer
        pageSize:
          type: integer
        orders:
          type: array
          items:
            $ref: '#/components/schemas/SenderOrderResponse'
    SenderStatsResponse:
      type: object
      properties:
        totalOrders:
          type: integer
        totalOrderVolume:
          type: string
        totalFeeEarnings:
          type: string

    # Provider Schemas
    ProviderOrderList:
      type: object
      properties:
        total:
          type: integer
        page:
          type: integer
        pageSize:
          type: integer
        orders:
          type: array
          items:
            $ref: '#/components/schemas/ProviderOrderResponse'
    ProviderOrderResponse:
      type: object
      properties:
        id:
          type: string
        token:
          type: string
          description: Token symbol (USDT, USDC, cNGN). See [Supported Stablecoins](/resources/supported-stablecoins) for available options.
        gatewayId:
          type: string
        amount:
          type: string
        amountInUsd:
          type: string
        rate:
          type: string
        blockNumber:
          type: integer
        txHash:
          type: string
        institution:
          type: string
          description: Institution code (SWIFT code or custom PayCrest code ending with 'PC'). See [Code Standards](/resources/code-standards) for details.
        accountIdentifier:
          type: string
        accountName:
          type: string
        providerId:
          type: string
        memo:
          type: string
        network:
          type: string
          enum: [ethereum, base, bnb-smart-chain, lisk, scroll, celo, arbitrum-one, polygon, asset-chain]
          description: Network identifier for the blockchain
        orderType:
          type: string
        status:
          type: string
        updatedAt:
          type: string
          format: date-time
        createdAt:
          type: string
          format: date-time
        transactionLogs:
          type: array
          items:
            $ref: '#/components/schemas/TransactionLog'
        cancellationReasons:
          type: array
          items:
            type: string
    AcceptOrderResponse:
      type: object
      properties:
        id:
          type: string
        amount:
          type: number
        institution:
          type: string
          description: Institution code (SWIFT code or custom PayCrest code ending with 'PC'). See [Code Standards](/resources/code-standards) for details.
        accountIdentifier:
          type: string
        accountName:
          type: string
        memo:
          type: string
        metadata:
          type: object
    FulfillLockOrderPayload:
      type: object
      required:
        - psp
        - txId
      properties:
        psp:
          type: string
        txId:
          type: string
        validationStatus:
          type: string
        validationError:
          type: string
    CancelLockOrderPayload:
      type: object
      required:
        - reason
      properties:
        reason:
          type: string
    MarketRateSide:
      type: object
      description: Tolerance band for one side (buy or sell) of the market rate.
      properties:
        marketRate:
          type: string
          description: Reference market rate for this side (fiat per crypto).
        minimumRate:
          type: string
          description: Minimum allowed rate for this side.
        maximumRate:
          type: string
          description: Maximum allowed rate for this side.
    MarketRateResponse:
      type: object
      description: Market rate bounds split by side. Either or both of `buy` and `sell` may be present depending on corridor configuration.
      properties:
        buy:
          $ref: '#/components/schemas/MarketRateSide'
        sell:
          $ref: '#/components/schemas/MarketRateSide'
    V2RateQuoteSide:
      type: object
      description: Quote details for one side from the public v2 rates endpoint.
      properties:
        rate:
          type: string
          description: Achievable rate (fiat per crypto) for this side and notional.
        providerIds:
          type: array
          items:
            type: string
          description: Provider id(s) tied to the quote (single id when `provider_id` was requested).
        orderType:
          type: string
          description: Order flow type, e.g. `regular` or `otc`.
        refundTimeoutMinutes:
          type: integer
          description: Minutes until automatic refund for this flow.
    V2RateQuoteResponse:
      type: object
      description: Public token rate quote. Omit `side` to receive both buy and sell; use `side=buy` or `side=sell` for a single side.
      properties:
        buy:
          $ref: '#/components/schemas/V2RateQuoteSide'
        sell:
          $ref: '#/components/schemas/V2RateQuoteSide'
    ProviderStatsResponse:
      type: object
      properties:
        totalOrders:
          type: integer
        totalFiatVolume:
          type: string
        totalCryptoVolume:
          type: string

    # General Schemas
    SupportedCurrencies:
      type: object
      properties:
        code:
          type: string
          description: ISO 4217 currency code (e.g., NGN, KES, UGX). See [Supported Currencies](/resources/supported-currencies) for available options.
        name:
          type: string
        shortName:
          type: string
        decimals:
          type: integer
        symbol:
          type: string
        marketBuyRate:
          type: string
        marketSellRate:
          type: string
    SupportedInstitutions:
      type: object
      properties:
        name:
          type: string
        code:
          type: string
          description: Institution code (SWIFT code or custom PayCrest code ending with 'PC'). See [Code Standards](/resources/code-standards) for details.
        type:
          type: string
    SupportedTokenResponse:
      type: object
      properties:
        symbol:
          type: string
          description: Token symbol (USDT, USDC, cNGN). See [Supported Stablecoins](/resources/supported-stablecoins) for available options.
        contractAddress:
          type: string
        decimals:
          type: integer
        baseCurrency:
          type: string
        network:
          type: string
          enum: [ethereum, base, bnb-smart-chain, lisk, scroll, celo, arbitrum-one, polygon, asset-chain]
          description: Network identifier for the blockchain

    # Order Status Schemas
    ProviderOrderStatusResponse:
      type: object
      properties:
        orderId:
          type: string
        amount:
          type: string
        amountInUsd:
          type: string
        token:
          type: string
          description: Token symbol (USDT, USDC, cNGN). See [Supported Stablecoins](/resources/supported-stablecoins) for available options.
        network:
          type: string
          enum: [ethereum, base, bnb-smart-chain, lisk, scroll, celo, arbitrum-one, polygon, asset-chain]
          description: Network identifier for the blockchain
        settlePercent:
          type: string
        status:
          type: string
        txHash:
          type: string
        settlements:
          type: array
          items:
            $ref: '#/components/schemas/PaymentOrderSplitOrder'
        txReceipts:
          type: array
          items:
            $ref: '#/components/schemas/PaymentOrderTxReceipt'
        updatedAt:
          type: string
          format: date-time
    PaymentOrderSplitOrder:
      type: object
      properties:
        splitOrderId:
          type: string
        amount:
          type: string
        rate:
          type: string
        orderPercent:
          type: string
    PaymentOrderTxReceipt:
      type: object
      properties:
        status:
          type: string
        txHash:
          type: string
        timestamp:
          type: string
          format: date-time

    # Common Schemas
    TransactionLog:
      type: object
      properties:
        id:
          type: string
        gateway_id:
          type: string
        status:
          type: string
        tx_hash:
          type: string
        created_at:
          type: string
          format: date-time

    # System Schemas
    VerifyAccountRequest:
      type: object
      required:
        - institution
        - accountIdentifier
      properties:
        institution:
          type: string
          description: Institution code (SWIFT code or custom PayCrest code ending with 'PC'). See [Code Standards](/resources/code-standards) for details.
        accountIdentifier:
          type: string
          description: |
            Bank account number, mobile wallet MSISDN, Till/Paybill identifier, or other account identifier.

            For **mobile_money** institutions, the API normalizes phone numbers before verification: strips `+`, removes a leading domestic `0`, and adds the country dial code when missing (KES 254, UGX 256, TZS 255, GHS 233, MWK 265). Examples for KES M-Pesa — `0712345678`, `+254712345678`, and `254712345678` all normalize to `254712345678`.

            KES Till/Paybill identifiers are not normalized when `metadata` indicates Till/Paybill, or when the value looks like Paybill (`business|reference`) or Till (fewer than 9 digits).
        metadata:
          type: object
          additionalProperties: true
          description: |
            Optional. Used for KES Till/Paybill verify so phone normalization is not applied. Same semantics as recipient `metadata` on order create (e.g. `channel` Till or Paybill, `businessNumber`).
    ErrorResponse:
      type: object
      properties:
        status:
          type: string
          example: error
        message:
          type: string
          example: Error message
        data:
          nullable: true
          description: "null on most errors; an array of field-level errors on validation failures"
          oneOf:
            - type: array
              items:
                type: object
                properties:
                  field:
                    type: string
                  message:
                    type: string
            - type: object
              additionalProperties: true
    StandardResponse:
      type: object
      properties:
        status:
          type: string
          example: success
        message:
          type: string
          example: Operation successful
        data:
          type: object
          description: The actual response data (e.g., ReceiveAddressResponse, etc.)

    # ─── V2 Schemas ────────────────────────────────────────────────────────────

    V2PaymentOrderPayload:
      type: object
      required:
        - amount
        - source
        - destination
      properties:
        amount:
          type: string
          description: Payment amount. Denomination is determined by `amountIn` (defaults to crypto units).
          example: "100"
        amountIn:
          type: string
          enum: [crypto, fiat]
          description: Specifies whether `amount` is denominated in crypto or fiat. Defaults to `crypto`.
        rate:
          type: string
          description: Quoted exchange rate (fiat per crypto unit). Optional — protocol uses best available rate if omitted.
        senderFee:
          type: string
          description: Fixed sender fee in crypto units.
        senderFeePercent:
          type: string
          description: Sender fee as a percentage of the order amount (alternative to senderFee).
        reference:
          type: string
          description: Your internal reference ID for this order.
        source:
          description: "Polymorphic source — set `type: \"crypto\"` for offramp or `type: \"fiat\"` for onramp."
          oneOf:
            - $ref: '#/components/schemas/V2CryptoSource'
            - $ref: '#/components/schemas/V2FiatSource'
          discriminator:
            propertyName: type
            mapping:
              crypto: '#/components/schemas/V2CryptoSource'
              fiat: '#/components/schemas/V2FiatSource'
        destination:
          description: "Polymorphic destination — set `type: \"fiat\"` for offramp or `type: \"crypto\"` for onramp."
          oneOf:
            - $ref: '#/components/schemas/V2FiatDestination'
            - $ref: '#/components/schemas/V2CryptoDestination'
          discriminator:
            propertyName: type
            mapping:
              fiat: '#/components/schemas/V2FiatDestination'
              crypto: '#/components/schemas/V2CryptoDestination'

    V2CryptoSource:
      type: object
      required: [type, currency, network, refundAddress]
      properties:
        type:
          type: string
          enum: [crypto]
        currency:
          type: string
          description: Stablecoin symbol (e.g. USDT, USDC, cNGN)
        network:
          type: string
          enum: [ethereum, base, bnb-smart-chain, lisk, scroll, celo, arbitrum-one, polygon]
        refundAddress:
          type: string
          description: Wallet address for refunds if the order can't be fulfilled

    V2FiatSource:
      type: object
      required: [type, currency, refundAccount]
      properties:
        type:
          type: string
          enum: [fiat]
        currency:
          type: string
          description: Fiat currency code (e.g. NGN, KES, BRL)
        country:
          type: string
          description: ISO 3166-1 alpha-2 country code (optional)
        refundAccount:
          $ref: '#/components/schemas/V2FiatRefundAccount'

    V2FiatRefundAccount:
      type: object
      required: [institution, accountIdentifier, accountName]
      properties:
        institution:
          type: string
          description: Bank or mobile provider code (SWIFT prefix or Paycrest institution code)
        institutionName:
          type: string
          description: Human-readable institution name (e.g. OPay). Present on GET responses when the code resolves in Paycrest.
        accountIdentifier:
          type: string
          description: Account number or mobile number
        accountName:
          type: string
          description: Account holder name

    V2FiatDestination:
      type: object
      required: [type, currency, recipient]
      properties:
        type:
          type: string
          enum: [fiat]
        currency:
          type: string
          description: Fiat currency code (e.g. NGN, KES)
        country:
          type: string
          description: ISO 3166-1 alpha-2 country code (optional)
        providerId:
          type: string
          description: Pin order to a specific provider (optional)
        kyc:
          type: object
          description: Optional destination (recipient) KYC for offramp. Do not send sender or provider KYB on offramp create.
        recipient:
          $ref: '#/components/schemas/V2FiatRecipient'

    V2FiatRecipient:
      type: object
      required: [institution, accountIdentifier, accountName, memo]
      properties:
        institution:
          type: string
          description: Institution code (e.g. SAFAKEPC for Safaricom M-Pesa). Till and Paybill use the same mobile institution with channel metadata.
        institutionName:
          type: string
          description: Human-readable institution name (e.g. OPay). Present on GET responses when the code resolves in Paycrest.
        accountIdentifier:
          type: string
          description: Bank account, mobile phone (E.164 or local), till number, or paybill account reference depending on channel.
        accountName:
          type: string
        memo:
          type: string
          description: Payment narration / reference
        metadata:
          type: object
          description: Corridor-specific hints. For KES mobile offramp, use channel and optional businessNumber (see Sender API Integration guide).
          properties:
            channel:
              type: string
              enum: [Mobile, Till, Paybill]
              description: KES payout channel. Mobile = phone M-Pesa; Till = buy goods; Paybill = paybill. Omit for default phone M-Pesa when accountIdentifier is a mobile number.
            businessNumber:
              type: string
              description: Paybill business number (required for Paybill when not inferrable from accountIdentifier alone).

    V2CryptoDestination:
      type: object
      required: [type, currency, recipient]
      properties:
        type:
          type: string
          enum: [crypto]
        currency:
          type: string
          description: Stablecoin symbol (e.g. USDT, USDC)
        providerId:
          type: string
        recipient:
          $ref: '#/components/schemas/V2CryptoRecipient'

    V2CryptoRecipient:
      type: object
      required: [address, network]
      properties:
        address:
          type: string
          description: Wallet address to receive stablecoins
        network:
          type: string
          enum: [ethereum, base, bnb-smart-chain, lisk, scroll, celo, arbitrum-one, polygon]

    V2CryptoProviderAccount:
      type: object
      description: Returned for offramp orders — the address to send stablecoins to
      properties:
        network:
          type: string
        receiveAddress:
          type: string
          description: EVM address to send stablecoins to
        validUntil:
          type: string
          format: date-time
          description: Deadline for the stablecoin deposit

    V2FiatProviderAccount:
      type: object
      description: Returned for onramp orders — the virtual account to deposit fiat into
      properties:
        institution:
          type: string
          description: Institution label for the virtual account (human-readable; from provider or stored metadata).
        accountIdentifier:
          type: string
          description: Account number or mobile number to send fiat to
        accountName:
          type: string
        amountToTransfer:
          type: string
          description: Exact fiat amount the user must deposit
        currency:
          type: string
          description: Fiat currency code
        validUntil:
          type: string
          format: date-time
          description: Deadline — order expires if fiat not received by this time

    V2PaymentOrderResponse:
      type: object
      description: Response returned when creating an order
      properties:
        id:
          type: string
          format: uuid
        status:
          type: string
        orderType:
          type: string
          enum: [regular, otc]
        timestamp:
          type: string
          format: date-time
        amount:
          type: string
          description: Crypto amount in token units
        rate:
          type: string
        senderFee:
          type: string
        senderFeePercent:
          type: string
        transactionFee:
          type: string
        reference:
          type: string
        providerAccount:
          description: Crypto provider account for offramp; fiat provider account (virtual account) for onramp
          oneOf:
            - $ref: '#/components/schemas/V2CryptoProviderAccount'
            - $ref: '#/components/schemas/V2FiatProviderAccount'
        source:
          oneOf:
            - $ref: '#/components/schemas/V2CryptoSource'
            - $ref: '#/components/schemas/V2FiatSource'
        destination:
          oneOf:
            - $ref: '#/components/schemas/V2FiatDestination'
            - $ref: '#/components/schemas/V2CryptoDestination'

    V2PaymentOrderGetResponse:
      type: object
      description: Response for a single order (or list item)
      properties:
        id:
          type: string
          format: uuid
        status:
          type: string
          enum: [initiated, deposited, pending, fulfilling, fulfilled, validated, settling, settled, cancelled, refunding, refunded, expired]
        orderType:
          type: string
          enum: [regular, otc]
        direction:
          type: string
          enum: [offramp, onramp]
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
        amount:
          type: string
        amountInUsd:
          type: string
        amountPaid:
          type: string
        amountReturned:
          type: string
        percentSettled:
          type: string
        rate:
          type: string
        senderFee:
          type: string
        senderFeePercent:
          type: string
        transactionFee:
          type: string
        reference:
          type: string
        txHash:
          type: string
        providerAccount:
          oneOf:
            - $ref: '#/components/schemas/V2CryptoProviderAccount'
            - $ref: '#/components/schemas/V2FiatProviderAccount'
        source:
          oneOf:
            - $ref: '#/components/schemas/V2CryptoSource'
            - $ref: '#/components/schemas/V2FiatSource'
        destination:
          oneOf:
            - $ref: '#/components/schemas/V2FiatDestination'
            - $ref: '#/components/schemas/V2CryptoDestination'

    V2PaymentOrderListResponse:
      type: object
      properties:
        total:
          type: integer
        page:
          type: integer
        pageSize:
          type: integer
        orders:
          type: array
          items:
            $ref: '#/components/schemas/V2PaymentOrderGetResponse'

    V2WebhookPayload:
      type: object
      description: Webhook payload for v2 payment order events
      properties:
        event:
          type: string
          enum: [payment_order.deposited, payment_order.pending, payment_order.validated, payment_order.settling, payment_order.settled, payment_order.refunding, payment_order.refunded, payment_order.expired]
        webhookVersion:
          type: string
          enum: ["2"]
        data:
          type: object
          properties:
            id:
              type: string
              format: uuid
            direction:
              type: string
              enum: [offramp, onramp]
            status:
              type: string
            amount:
              type: string
            amountInUsd:
              type: string
            amountPaid:
              type: string
            amountReturned:
              type: string
            percentSettled:
              type: string
            senderFee:
              type: string
            transactionFee:
              type: string
            rate:
              type: string
            gatewayId:
              type: string
            senderId:
              type: string
              format: uuid
            providerAccount:
              oneOf:
                - $ref: '#/components/schemas/V2CryptoProviderAccount'
                - $ref: '#/components/schemas/V2FiatProviderAccount'
            source:
              oneOf:
                - $ref: '#/components/schemas/V2CryptoSource'
                - $ref: '#/components/schemas/V2FiatSource'
            destination:
              oneOf:
                - $ref: '#/components/schemas/V2FiatDestination'
                - $ref: '#/components/schemas/V2CryptoDestination'
            fromAddress:
              type: string
            reference:
              type: string
            txHash:
              type: string
            timestamp:
              type: string
              format: date-time

    WebhookDelivery:
      type: object
      description: |
        A single outbound webhook delivery attempt. Returned in list responses
        (the large `requestPayload` and `responseBody` fields are omitted from list
        items — fetch a single delivery to see them).
      properties:
        id:
          type: string
          format: uuid
          description: Unique ID of this delivery record.
        event:
          type: string
          enum: [payment_order.deposited, payment_order.pending, payment_order.validated, payment_order.settling, payment_order.settled, payment_order.refunding, payment_order.refunded, payment_order.expired]
          description: The webhook event that was delivered.
        eventId:
          type: string
          description: Identifier of the underlying event (matches the `event_id` filter).
        orderId:
          type: string
          format: uuid
          description: The payment order this delivery relates to, if any.
        status:
          type: string
          enum: [pending, success, failed, expired]
          description: |
            Delivery status. `pending` — queued or awaiting retry; `success` — the
            endpoint returned a 2xx; `failed` — a transport error or non-2xx response,
            scheduled for retry; `expired` — gave up after the 24h retry window.
        trigger:
          type: string
          enum: [automatic, cron_retry, manual_sync, manual_async]
          description: |
            What produced this attempt. `automatic` — first send on the event;
            `cron_retry` — automatic backoff retry; `manual_sync`/`manual_async` —
            a sender-initiated retry via the retry endpoint.
        httpStatusCode:
          type: integer
          description: HTTP status code returned by your endpoint (0 if no response was received).
          example: 200
        attemptNumber:
          type: integer
          description: 1-based attempt counter for this delivery.
          example: 1
        destinationUrl:
          type: string
          format: uri
          description: The webhook URL the request was sent to.
        signature:
          type: string
          description: The `X-Paycrest-Signature` (HMAC-SHA256 hex) sent with the request.
        errorMessage:
          type: string
          description: Transport or HTTP error message, if the attempt failed.
        durationMs:
          type: integer
          description: Round-trip duration of the attempt, in milliseconds.
        nextRetryTime:
          type: string
          format: date-time
          description: When the next automatic retry is scheduled (null once delivered or expired).
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time

    WebhookDeliveryDetail:
      description: A single webhook delivery including the frozen request payload and captured response body.
      allOf:
        - $ref: '#/components/schemas/WebhookDelivery'
        - type: object
          properties:
            requestPayload:
              type: object
              description: The exact JSON body that was (or will be) sent, frozen at first attempt.
            responseBody:
              type: string
              description: Response body returned by your endpoint, truncated to 4KB.

    WebhookDeliveryList:
      type: object
      properties:
        total:
          type: integer
        page:
          type: integer
        pageSize:
          type: integer
        deliveries:
          type: array
          items:
            $ref: '#/components/schemas/WebhookDelivery'

    WebhookRetryResult:
      type: object
      description: |
        Result of a synchronous retry (`?sync=true`). Reports the HTTP outcome of
        replaying the original payload inline.
      properties:
        id:
          type: string
          format: uuid
          description: ID of the delivery record created for this retry.
        status:
          type: string
          enum: [success, failed]
        httpStatusCode:
          type: integer
          example: 200
        durationMs:
          type: integer
        errorMessage:
          type: string



servers:
  - url: https://api.paycrest.io/v2
paths:

  /sender/stats:
    get:
      tags:
        - Sender
      summary: Get sender stats
      security:
        - ApiKeyAuth: []
      servers:
        - url: https://api.paycrest.io/v2
      parameters:
        - $ref: '#/components/parameters/OptionalOrderDirection'
      responses:
        '200':
          description: Sender stats
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/StandardResponse'
                  - type: object
                    properties:
                      data:
                        $ref: '#/components/schemas/SenderStatsResponse'
        '400':
          description: Bad request (e.g. invalid `direction`)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /orders/{chain_id}/{id}:
    get:
      tags:
        - General
      summary: Get order status by Gateway ID
      description: Retrieve the status of an order by its onchain Gateway ID and chain ID. Use this for smart contract integrations.
      servers:
        - url: https://api.paycrest.io/v2
      parameters:
        - in: path
          name: chain_id
          required: true
          schema:
            type: string
        - in: path
          name: id
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Order status
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/StandardResponse'
                  - type: object
                    properties:
                      data:
                        $ref: '#/components/schemas/ProviderOrderStatusResponse'
        '400':
          description: Bad request
        '404':
          description: Not found

  # ─── V2 Provider Action Endpoints ────────────────────────────────────────────

  /provider/orders/{id}/accept:
    post:
      tags:
        - Provider
      summary: Accept an order
      description: Provider accepts an assigned order, signalling intent to fulfill.
      security:
        - ApiKeyAuth: []
      servers:
        - url: https://api.paycrest.io/v2
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Order accepted
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardResponse'
        '400':
          description: Bad request
        '401':
          description: Unauthorized
        '404':
          description: Order not found

  /provider/orders/{id}/decline:
    post:
      tags:
        - Provider
      summary: Decline an order
      description: Provider declines an assigned order. The aggregator will reassign it to another provider.
      security:
        - ApiKeyAuth: []
      servers:
        - url: https://api.paycrest.io/v2
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Order declined
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardResponse'
        '400':
          description: Bad request
        '401':
          description: Unauthorized
        '404':
          description: Order not found

  /provider/orders/{id}/fulfill:
    post:
      tags:
        - Provider
      summary: Fulfill an order
      description: Provider reports that the fiat payment has been disbursed to the recipient.
      security:
        - ApiKeyAuth: []
      servers:
        - url: https://api.paycrest.io/v2
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                txId:
                  type: string
                  description: PSP transaction ID or reference for the payout
      responses:
        '200':
          description: Order fulfilled
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardResponse'
        '400':
          description: Bad request
        '401':
          description: Unauthorized
        '404':
          description: Order not found

  /provider/orders/{id}/cancel:
    post:
      tags:
        - Provider
      summary: Cancel an order
      description: Provider cancels a previously accepted order. The aggregator will attempt reassignment.
      security:
        - ApiKeyAuth: []
      servers:
        - url: https://api.paycrest.io/v2
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                reason:
                  type: string
                  description: Reason for cancellation
      responses:
        '200':
          description: Order cancelled
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardResponse'
        '400':
          description: Bad request
        '401':
          description: Unauthorized
        '404':
          description: Order not found

  /provider/balances:
    post:
      tags:
        - Provider
      summary: Update provider balances
      description: Provider node reports current available liquidity balances per currency.
      security:
        - ApiKeyAuth: []
      servers:
        - url: https://api.paycrest.io/v2
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                balances:
                  type: array
                  items:
                    type: object
                    required:
                      - currency
                      - availableBalance
                    properties:
                      currency:
                        type: string
                        description: Fiat currency code (e.g. NGN, KES)
                      availableBalance:
                        type: string
                        description: Available balance in fiat units
      responses:
        '200':
          description: Balances updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardResponse'
        '400':
          description: Bad request
        '401':
          description: Unauthorized

  /provider/rates/{token}/{fiat}:
    get:
      tags:
        - Provider
      summary: Get market rate
      security:
        - ApiKeyAuth: []
      servers:
        - url: https://api.paycrest.io/v2
      parameters:
        - in: path
          name: token
          required: true
          schema:
            type: string
        - in: path
          name: fiat
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Market rate
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/StandardResponse'
                  - type: object
                    properties:
                      data:
                        $ref: '#/components/schemas/MarketRateResponse'
        '400':
          description: Bad request
        '401':
          description: Unauthorized

  /provider/stats:
    get:
      tags:
        - Provider
      summary: Get provider stats
      security:
        - ApiKeyAuth: []
      servers:
        - url: https://api.paycrest.io/v2
      parameters:
        - in: query
          name: currency
          required: true
          schema:
            type: string
          description: Fiat currency code (e.g. NGN, KES).
        - $ref: '#/components/parameters/OptionalOrderDirection'
      responses:
        '200':
          description: Provider stats
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/StandardResponse'
                  - type: object
                    properties:
                      data:
                        $ref: '#/components/schemas/ProviderStatsResponse'
        '400':
          description: Bad request (e.g. missing `currency`, invalid `direction`)
        '401':
          description: Unauthorized

  /provider/node-info:
    get:
      tags:
        - Provider
      summary: Get node info
      security:
        - ApiKeyAuth: []
      servers:
        - url: https://api.paycrest.io/v2
      responses:
        '200':
          description: Node info
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/StandardResponse'
                  - type: object
                    properties:
                      data:
                        type: object
        '401':
          description: Unauthorized
        '503':
          description: Service unavailable

  # ─── V2 General Endpoints ─────────────────────────────────────────────────────

  /currencies:
    get:
      tags:
        - General
      summary: List supported fiat currencies
      servers:
        - url: https://api.paycrest.io/v2
      responses:
        '200':
          description: List of currencies
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: success
                  message:
                    type: string
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/SupportedCurrencies'

  /institutions/{currency_code}:
    get:
      tags:
        - General
      summary: List supported institutions for a currency
      servers:
        - url: https://api.paycrest.io/v2
      parameters:
        - in: path
          name: currency_code
          required: true
          schema:
            type: string
      responses:
        '200':
          description: List of institutions
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: success
                  message:
                    type: string
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/SupportedInstitutions'
        '400':
          description: Bad request

  /tokens:
    get:
      tags:
        - General
      summary: List supported tokens
      servers:
        - url: https://api.paycrest.io/v2
      parameters:
        - in: query
          name: network
          schema:
            type: string
            enum: [ethereum, base, bnb-smart-chain, lisk, scroll, celo, arbitrum-one, polygon, asset-chain]
      responses:
        '200':
          description: List of tokens
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: success
                  message:
                    type: string
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/SupportedTokenResponse'

  /rates/{network}/{from}/{amount}/{to}:
    get:
      tags:
        - General
      summary: Get token rate (v2)
      description: |
        Public quote on a specific network. Returns **buy** and/or **sell** quotes, order type, and refund timeout.

        **Token + fiat:** each of **`from`** / **`to`** is either an enabled **fiat currency code** or an enabled **crypto (token) symbol** on **`network`**. Exactly one must be fiat and one crypto (`.../USDT/100/NGN` and `.../NGN/100/USDT` are equivalent). **`amount`** is the **crypto notional**. Rates are **fiat per 1 token** for each side. **`from` and `to` cannot both be crypto.**

        **Ambiguous segment:** if a path value matches **both** an enabled fiat code **and** an enabled crypto symbol on that network (e.g. same string), the request returns **400** unless **`from_source`** and/or **`to_source`** is set to **`fiat`** or **`crypto`** to pick the leg.

        **Fiat + fiat:** both segments are fiat codes. The quote **bridges through USDC** on the same **`network`**. **`amount`** is denominated in **`from`** fiat. **`sell`** is **destination fiat per 1 unit of `from` fiat** (buy USDC with `from`, sell USDC for `to`). **`buy`** is **`from` fiat per 1 unit of `to` fiat** (inverse corridor; buy USDC with `to`, sell USDC for `from`)—asymmetric from **`sell`**, not a simple reciprocal.

        Path order does **not** imply trade direction for token/fiat pairs; omitting **`side`** returns **both** sides when available.

        For the legacy single-number response, use **`GET /v1/rates/{token}/{amount}/{fiat}`** with optional `network` query.
      servers:
        - url: https://api.paycrest.io/v2
      parameters:
        - in: path
          name: network
          required: true
          schema:
            type: string
            enum: [ethereum, base, bnb-smart-chain, lisk, scroll, celo, arbitrum-one, polygon, asset-chain]
          description: Blockchain network identifier (lowercase in URL).
        - in: path
          name: from
          required: true
          schema:
            type: string
          description: Fiat code or token symbol on `network` (paired with `to`).
        - in: path
          name: amount
          required: true
          schema:
            type: string
          description: For token+fiat pairs, the **token** notional. For fiat+fiat pairs, the amount in **`from`** fiat.
        - in: path
          name: to
          required: true
          schema:
            type: string
          description: Fiat code or token symbol on `network` (paired with `from`).
        - in: query
          name: side
          schema:
            type: string
            enum: [buy, sell]
          description: Return only the buy or sell quote. Omit for both sides.
        - in: query
          name: from_source
          schema:
            type: string
            enum: [fiat, crypto]
          description: When `from` matches both a fiat and a crypto asset on `network`, set to `fiat` or `crypto` to disambiguate. Ignored when `from` is unambiguous.
        - in: query
          name: to_source
          schema:
            type: string
            enum: [fiat, crypto]
          description: When `to` matches both a fiat and a crypto asset on `network`, set to `fiat` or `crypto` to disambiguate. Ignored when `to` is unambiguous.
        - in: query
          name: provider_id
          schema:
            type: string
            pattern: '^[A-Za-z]{8}$'
          description: Optional. When set, limits the quote to this provider. Must be exactly 8 alphabetic characters (A–Z, a–z).
      responses:
        '200':
          description: Token rate quote(s)
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/StandardResponse'
                  - type: object
                    properties:
                      data:
                        $ref: '#/components/schemas/V2RateQuoteResponse'
        '400':
          description: Bad request (invalid amount, side, provider_id, ambiguous from/to without from_source/to_source, unsupported asset/network, etc.)
        '404':
          description: No provider available for the requested swap
        '500':
          description: Internal error while resolving rate
        '503':
          description: Temporary provider or banking/network issue for the currency

  /pubkey:
    get:
      tags:
        - General
      summary: Get aggregator public key
      servers:
        - url: https://api.paycrest.io/v2
      responses:
        '200':
          description: Public key
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: success
                  message:
                    type: string
                  data:
                    type: string

  /verify-account:
    post:
      tags:
        - General
      summary: Verify account
      description: |
        Resolves the account holder name when available. For some fiat corridors (e.g. KES M-Pesa), `data` may be `"OK"` when the account is valid but no display name is returned — supply your own `accountName` on the order in that case.
      servers:
        - url: https://api.paycrest.io/v2
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/VerifyAccountRequest'
            examples:
              ngnBank:
                summary: NGN bank account
                value:
                  institution: GTBINGLA
                  accountIdentifier: "0123456789"
              kesMobile:
                summary: KES M-Pesa (local MSISDN normalized server-side)
                value:
                  institution: SAFAKEPC
                  accountIdentifier: "0712345678"
              kesTill:
                summary: KES Till (metadata skips phone normalization)
                value:
                  institution: SAFAKEPC
                  accountIdentifier: "123456"
                  metadata:
                    channel: Till
      responses:
        '200':
          description: Account verified
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: success
                  message:
                    type: string
                    example: Account name was fetched successfully
                  data:
                    type: string
                    description: Resolved account name, or `"OK"` when valid but name lookup is unavailable
                    example: JOHN DOE
        '400':
          description: Bad request
        '502':
          description: Bad gateway
        '503':
          description: Service unavailable

  /reindex/{network}/{tx_hash_or_address}:
    get:
      tags:
        - General
      summary: Reindex transaction
      servers:
        - url: https://api.paycrest.io/v2
      parameters:
        - in: path
          name: network
          required: true
          schema:
            type: string
        - in: path
          name: tx_hash_or_address
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Transaction reindexed
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/StandardResponse'
                  - type: object
                    properties:
                      data:
                        type: object
                        properties:
                          events:
                            type: object
        '400':
          description: Bad request

  # ─── V2 Sender Endpoints ─────────────────────────────────────────────────────

  /sender/orders:
    post:
      tags:
        - Sender
      summary: Create a v2 payment order (offramp or onramp)
      description: |
        Creates a new payment order.

        - **Offramp** (stablecoin → fiat): set `source.type = "crypto"` and `destination.type = "fiat"`.
          The response `providerAccount` contains a `receiveAddress` to send tokens to.
        - **Onramp** (fiat → stablecoin): set `source.type = "fiat"` and `destination.type = "crypto"`.
          The response `providerAccount` contains a virtual bank account for the user to deposit fiat into.
      security:
        - ApiKeyAuth: []
      servers:
        - url: https://api.paycrest.io/v2
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/V2PaymentOrderPayload'
            examples:
              offramp:
                summary: Offramp — USDT on Base → NGN bank account
                value:
                  amount: "100"
                  source:
                    type: crypto
                    currency: USDT
                    network: base
                    refundAddress: "0xYourAddress"
                  destination:
                    type: fiat
                    currency: NGN
                    recipient:
                      institution: GTBINGLA
                      accountIdentifier: "1234567890"
                      accountName: John Doe
                      memo: Payment
              onramp:
                summary: Onramp — NGN → USDT on Base
                value:
                  amount: "50000"
                  amountIn: fiat
                  source:
                    type: fiat
                    currency: NGN
                    refundAccount:
                      institution: GTBINGLA
                      accountIdentifier: "1234567890"
                      accountName: John Doe
                  destination:
                    type: crypto
                    currency: USDT
                    recipient:
                      address: "0xRecipientWalletAddress"
                      network: base
      responses:
        '201':
          description: Order created
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: success
                  message:
                    type: string
                    example: Payment order created successfully
                  data:
                    $ref: '#/components/schemas/V2PaymentOrderResponse'
        '400':
          description: Validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '503':
          description: No provider available for this corridor
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
    get:
      tags:
        - Sender
      summary: List v2 payment orders
      security:
        - ApiKeyAuth: []
      servers:
        - url: https://api.paycrest.io/v2
      parameters:
        - in: query
          name: page
          schema:
            type: integer
            default: 1
        - in: query
          name: pageSize
          schema:
            type: integer
            default: 10
        - in: query
          name: status
          schema:
            type: string
            enum: [initiated, deposited, pending, fulfilling, fulfilled, validated, settling, settled, cancelled, refunding, refunded, expired]
        - $ref: '#/components/parameters/OptionalOrderDirection'
      responses:
        '200':
          description: List of orders
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                  data:
                    $ref: '#/components/schemas/V2PaymentOrderListResponse'
        '401':
          description: Unauthorized

  /sender/orders/{id}:
    get:
      tags:
        - Sender
      summary: Get a v2 payment order by ID
      security:
        - ApiKeyAuth: []
      servers:
        - url: https://api.paycrest.io/v2
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: V2 payment order
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                  data:
                    $ref: '#/components/schemas/V2PaymentOrderGetResponse'
        '401':
          description: Unauthorized
        '404':
          description: Order not found

  /sender/webhooks:
    get:
      tags:
        - Sender
      summary: List webhook deliveries
      description: |
        Returns the authenticated sender's webhook delivery log, newest first.
        Use this to audit which events were delivered and to find failed or expired
        deliveries to replay. List items omit the large `requestPayload` and
        `responseBody` fields — fetch a single delivery to see them.
      security:
        - ApiKeyAuth: []
      servers:
        - url: https://api.paycrest.io/v2
      parameters:
        - in: query
          name: status
          schema:
            type: string
            enum: [pending, success, failed, expired]
          description: Filter by delivery status.
        - in: query
          name: event
          schema:
            type: string
          description: Filter by event name, e.g. `payment_order.settled`.
        - in: query
          name: event_id
          schema:
            type: string
          description: Filter by the underlying event identifier.
        - in: query
          name: order_id
          schema:
            type: string
            format: uuid
          description: Filter to deliveries for a single payment order.
        - in: query
          name: from
          schema:
            type: string
            format: date-time
          description: Only deliveries created at or after this timestamp.
        - in: query
          name: to
          schema:
            type: string
            format: date-time
          description: Only deliveries created at or before this timestamp.
        - in: query
          name: page
          schema:
            type: integer
            default: 1
        - in: query
          name: pageSize
          schema:
            type: integer
            default: 10
      responses:
        '200':
          description: List of webhook deliveries
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                  data:
                    $ref: '#/components/schemas/WebhookDeliveryList'
        '401':
          description: Unauthorized

  /sender/webhooks/{id}:
    get:
      tags:
        - Sender
      summary: Get a webhook delivery by ID
      description: |
        Returns a single webhook delivery, including the frozen `requestPayload`
        and the captured `responseBody`. Returns `404` if the delivery belongs to
        another sender.
      security:
        - ApiKeyAuth: []
      servers:
        - url: https://api.paycrest.io/v2
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Webhook delivery
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                  data:
                    $ref: '#/components/schemas/WebhookDeliveryDetail'
        '401':
          description: Unauthorized
        '404':
          description: Delivery not found

  /sender/webhooks/{id}/retry:
    post:
      tags:
        - Sender
      summary: Retry a webhook delivery
      description: |
        Replays a webhook delivery using its original frozen payload.

        - **Synchronous** (`?sync=true`): the payload is re-sent immediately and the
          HTTP result is returned inline (`200`).
        - **Asynchronous** (default): a new `pending` delivery is queued for the retry
          worker and the new delivery ID is returned (`202`).

        Returns `404` if the delivery belongs to another sender.
      security:
        - ApiKeyAuth: []
      servers:
        - url: https://api.paycrest.io/v2
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
            format: uuid
        - in: query
          name: sync
          schema:
            type: boolean
            default: false
          description: When `true`, replay immediately and return the HTTP result inline.
      responses:
        '200':
          description: Synchronous retry result
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                  data:
                    $ref: '#/components/schemas/WebhookRetryResult'
        '202':
          description: Asynchronous retry queued
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: success
                  message:
                    type: string
                    example: Webhook retry queued
                  data:
                    type: object
                    properties:
                      id:
                        type: string
                        format: uuid
                        description: ID of the newly queued delivery.
        '401':
          description: Unauthorized
        '404':
          description: Delivery not found

  # ─── V2 Provider Endpoints ────────────────────────────────────────────────────

  /provider/orders:
    get:
      tags:
        - Provider
      summary: List v2 payment orders (provider)
      description: Returns a paginated list of payment orders assigned to the authenticated provider, in the v2 response schema. Requires a `currency` query parameter. Supports optional `direction` (`onramp` / `offramp`), `search`, `export=csv`, `from`/`to` date filters, and `ordering` (asc/desc).
      security:
        - ApiKeyAuth: []
      servers:
        - url: https://api.paycrest.io/v2
      parameters:
        - in: query
          name: currency
          required: true
          schema:
            type: string
            example: NGN
          description: Fiat currency to filter orders by (e.g. NGN, KES).
        - in: query
          name: page
          schema:
            type: integer
            default: 1
        - in: query
          name: pageSize
          schema:
            type: integer
            default: 10
        - in: query
          name: status
          schema:
            type: string
            enum: [initiated, deposited, pending, fulfilling, fulfilled, validated, settling, settled, cancelled, refunding, refunded, expired]
        - in: query
          name: ordering
          schema:
            type: string
            enum: [asc, desc]
            default: desc
        - in: query
          name: search
          schema:
            type: string
          description: Search by order ID, reference, or account identifier.
        - in: query
          name: export
          schema:
            type: string
            enum: [csv]
          description: Pass `export=csv` with `from` and `to` to download a CSV export.
        - in: query
          name: from
          schema:
            type: string
            format: date
          description: Start date for CSV export (YYYY-MM-DD). Required when `export=csv`.
        - in: query
          name: to
          schema:
            type: string
            format: date
          description: End date for CSV export (YYYY-MM-DD). Required when `export=csv`.
        - $ref: '#/components/parameters/OptionalOrderDirection'
      responses:
        '200':
          description: Paginated list of orders
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: success
                  message:
                    type: string
                  data:
                    $ref: '#/components/schemas/V2PaymentOrderListResponse'
        '400':
          description: Bad request (e.g. missing currency)
        '401':
          description: Unauthorized

  /provider/orders/{id}:
    get:
      tags:
        - Provider
      summary: Get a v2 payment order by ID (provider)
      description: Returns a single payment order assigned to the authenticated provider, including polymorphic source/destination, providerAccount, and transaction logs.
      security:
        - ApiKeyAuth: []
      servers:
        - url: https://api.paycrest.io/v2
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
            format: uuid
          description: UUID of the payment order.
      responses:
        '200':
          description: V2 payment order
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: success
                  message:
                    type: string
                  data:
                    $ref: '#/components/schemas/V2PaymentOrderGetResponse'
        '401':
          description: Unauthorized
        '404':
          description: Order not found
